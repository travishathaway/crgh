[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "crgh"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "click",
    "requests",
]

[project.scripts]
crgh = "crgh.cli:main"
